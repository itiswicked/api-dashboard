<div class="small-6 small-offset-3 columns">
  <div class="callout primary">
    <h3>Events in <%= @location.city %>, <%= @location.state %></h3>

    <ul>
      <% @events.each do |event| %>
        <li><%= event %></li>
      <% end %>
    </ul>
  </div>
</div>
